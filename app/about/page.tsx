import Image from "next/image";import { PageFrame } from "@/components/page-frame";import { company,capacityFacts } from "@/lib/data/company";import { mediaBy } from "@/lib/customer-media"
export const metadata={title:"About",description:"Learn about Shanghai Brightstone Medical Technology Limited and its RF medical-component manufacturing capabilities.",alternates:{canonical:"https://shbsmed.com/about"}}
export default function About(){const photos=mediaBy('公司入户区','办公室','展会照片');return <PageFrame eyebrow="About Brightstone" title="A Shanghai medical-component manufacturer focused on RF procedures" description="Founded in December 2015, Brightstone Medical combines dedicated product lines, custom needle-component capability and international export experience."><section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{capacityFacts.map(x=><article className="rounded-xl border p-6" key={x.label.en}><strong className="text-2xl text-brand-blue-deep">{x.value.en}</strong><h2 className="mt-2 font-semibold">{x.label.en}</h2><p className="mt-2 text-sm text-muted-foreground">{x.helper.en}</p></article>)}</div><p className="mt-12 max-w-4xl text-lg leading-8 text-muted-foreground">{company.legalNameEn} operates from {company.address}. The customer-supplied company profile records continuous exports to the United States for ten years and medical-device registrations for key markets.</p><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{photos.map((m,i)=><figure className="relative aspect-[4/3] overflow-hidden rounded-xl" key={m.url}><Image src={m.url} alt={`Brightstone company and exhibition evidence ${i+1}`} fill className="object-cover"/></figure>)}</div></section></PageFrame>}
