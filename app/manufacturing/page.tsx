import Image from "next/image";import { PageFrame } from "@/components/page-frame";import { processVideos } from "@/lib/data/media";import { productionVideos,mediaBy } from "@/lib/customer-media"
export const metadata={title:"Manufacturing",description:"View real production footage and facility evidence from Brightstone Medical.",alternates:{canonical:"https://shbsmed.com/manufacturing"}}
export default function Manufacturing(){const photos=mediaBy('净化车间','全自动打印机','生产过程中的针管','半成品针');return <PageFrame eyebrow="Manufacturing" title="Visible process control from cutting to finished components" description="Real production footage and facility evidence document the processes behind Brightstone Medical products."><section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><div className="grid gap-8 lg:grid-cols-2">{processVideos.map((v,i)=><article className="overflow-hidden rounded-2xl border" key={v.id}><video className="aspect-video w-full bg-slate-950 object-cover" poster={v.poster} src={productionVideos[i]?.url} muted loop playsInline controls preload="metadata"/><div className="p-6"><h2 className="text-xl font-semibold">{v.title}</h2><p className="mt-2 text-muted-foreground">{v.description}</p></div></article>)}</div><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{photos.map((m,i)=><figure className="overflow-hidden rounded-xl border bg-white" key={m.url}><div className="relative aspect-[4/3]"><Image src={m.url} alt={`Brightstone manufacturing evidence ${i+1}`} fill className="object-cover"/></div></figure>)}</div></section></PageFrame>}
