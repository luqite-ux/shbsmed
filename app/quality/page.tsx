import Image from "next/image";import { PageFrame } from "@/components/page-frame";import { mediaBy } from "@/lib/customer-media";const steps=['Incoming material identification','Precision cutting and forming','In-process dimensional checks','Cleanroom assembly and printing','Finished-product inspection and release']
export const metadata={title:"Quality",description:"See Brightstone Medical inspection areas and production-stage quality controls.",alternates:{canonical:"https://shbsmed.com/quality"}}
export default function Quality(){const photos=mediaBy('检验室','成品');return <PageFrame eyebrow="Quality" title="Inspection integrated into each production stage" description="Brightstone Medical uses dedicated inspection areas and documented process checks across its RF component lines."><section className="mx-auto max-w-5xl px-4 py-16 sm:px-6 lg:px-8"><ol className="grid gap-5">{steps.map((s,i)=><li className="flex gap-5 rounded-xl border p-6" key={s}><span className="flex size-10 shrink-0 items-center justify-center rounded-full bg-brand-blue text-white">{i+1}</span><div><h2 className="font-semibold">{s}</h2><p className="mt-1 text-sm text-muted-foreground">Recorded and reviewed as part of the applicable production workflow.</p></div></li>)}</ol><div className="mt-12 grid gap-5 sm:grid-cols-2">{photos.map((m,i)=><figure className="relative aspect-[4/3] overflow-hidden rounded-xl" key={m.url}><Image src={m.url} alt={`Inspection and finished-goods evidence ${i+1}`} fill className="object-cover"/></figure>)}</div></section></PageFrame>}
