import type { Metadata } from "next";import { notFound } from "next/navigation";import { PageFrame } from "@/components/page-frame";import { getArticleBySlug } from "@/lib/articles-db";export const dynamicParams=true;export const revalidate=60
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,a=await getArticleBySlug(slug);if(!a)return{};const url=`https://shbsmed.com/news/${slug}`;return{title:a.title,description:a.excerpt,alternates:{canonical:url},openGraph:{title:a.title,description:a.excerpt,url,type:"article",images:a.image?[a.image]:[]}}}
export default async function NewsArticle({params}:{params:Promise<{slug:string}>}){const{slug}=await params,a=await getArticleBySlug(slug);if(!a)notFound();const url=`https://shbsmed.com/news/${slug}`,schema={"@context":"https://schema.org","@type":"Article","@id":`${url}#article`,headline:a.title,description:a.excerpt,datePublished:a.publishedAt,dateModified:a.updatedAt,mainEntityOfPage:url,publisher:{"@id":"https://shbsmed.com/#organization"},...(a.image?{image:a.image}:{})};return <PageFrame eyebrow="Insight" title={a.title} description={a.excerpt}><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><article className="article-prose mx-auto max-w-3xl px-4 py-16 sm:px-6" dangerouslySetInnerHTML={{__html:a.content}}/></PageFrame>}
